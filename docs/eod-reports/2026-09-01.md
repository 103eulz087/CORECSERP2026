# EOD Report — 2026-09-01

One commit today: `db5002f eulzlaptop_09012026_0658_PM`. Working tree is clean relative to it.

**Note on authorship:** this commit was made in the laptop environment, not through this (desktop) session. This session itself made no code changes today — the notes below summarize what actually landed in the repo today, reconstructed from `git show`/`git diff`, per this report's own instructions not to invent work.

## Completed

*(all via the laptop-side commit above)*

- **`ClientPaymentsDevExAcctg.cs` + `SQL/2026-09-01_ClientPayment_ServicesAmount.sql` — new "ServicesAmount" charge.**
  Adds an additive fee (e.g. cutting charge) billed on top of the invoice balance being collected, credited to AR-Others instead of AR-Trade. Threaded through the grid (footer total, editable column, recalculation, validation ceiling) and the SP's GL split. `gross` reconstruction explicitly subtracts both `overpay` and `servicesAmount` back out to avoid double-counting into the invoice-settlement leg — comments call this out directly.

- **`ConversionPerBarcode.cs` + 3 new SQL files — three related Conversion fixes:**
  - *Driploss now creates real sellable Inventory* (`...DriplossCreatesSellableInventory.sql`) — driploss output lines were previously audit-only (no `Inventory` row, `NewInventorySeqNo`/`NewBarcode` stayed NULL); now inserted at `Cost = 0`. Side effect: driploss lots now participate in `spu_ReverseConversionBarcode`'s generic join (no `IsDriploss` filter there), called out as intentional.
  - *Parent ReferenceCode propagation for Manual-shipment sourcing* (`...ParentReferenceCodeForManualShipment.sql`) — output lots from "Select Product (FIFO)" → "Manual (By Shipment)" now carry the consumed parent lot's own `ReferenceCode` instead of the `ConversionRefNo`, so output is traceable to its source batch. Barcode-scan/Auto sourcing unchanged (no single unambiguous parent lot to propagate from). Required dropping/recreating `tt_ConversionBarcodeSourceLines` (table types can't `ALTER ADD COLUMN`) — verified via `sys.parameters`/`sys.table_types` that only the live proc referenced it before doing so.
  - *Percentage-per-share cost allocation* (`...PercentagePerShareFormula.sql`) — replaces the old flat per-unit-cost allocation across non-driploss output lines with a proportional-to-qty share. Verified against a user-supplied worked example (4 source lots, 3 output lines incl. driploss) in the file header.
  - `ConversionPerBarcode.cs` diff (12 lines) just threads the new `ReferenceCode` column through the source grid/TVP for the Manual-shipment case.

- **`SQL/2026-09-01_sp_ReplicateSales_DateRange.sql`** — `sp_ReplicateSales` converted from a single-date `@petsa` param to a `@FromDate`/`@ToDate` range, so `POSDataManagementAutomation.cs` can replicate a whole selected span in one call instead of once per distinct date.

- **`SQL/2026-09-01_POSDataManagementAutomation_FilterParameters.sql` + `POSDataManagementAutomation.cs`/`.Designer.cs`, `Main.cs`/`.Designer.cs`/`.resx`** — `sp_GetSalesDateForDataProcessing` had zero parameters (including a dead commented-out hardcoded test filter), so the auto-processing cycle always pulled every unprocessed batch system-wide, ignoring the Branch/Sales-From/Sales-To/Machine fields on the form. Added the missing (all-optional) filter params. New ribbon entry `barbtnPOSAutomation_ItemClick` wires the form into `Main.cs`.

- **`AddBranchOrder.cs`** (STS module, 4 lines) — the pending-items grid query switched from filtering by `DeliveryNo` to `PONumber`, and the cancel/return exclusion broadened from `isCancelled=0` to `isnull(isCancelled,0)=0 AND isnull(isReturned,0)=0` (NULL-safe, and now also excludes returned lines, not just cancelled ones).

- Also touched but not reviewed in depth today (see Open Questions): `POSManagementReport.cs`, `POSXReadReportDevEx.cs`, `Orders/ViewBranchOrder.Designer.cs`/`.resx`, `.csproj`/`.csproj.user`/`.sln` housekeeping, build artifacts (`bin`/`obj`).

## In Progress

Nothing in progress from this session — no files were edited today via this conversation. (For context: the prior session's Dispatch Per Barcode dynamic-origin-branch work is already committed as of 2026-08-25, `f62c744`/`d14292f` — not part of today's activity.)

## Bugs Encountered (known patterns)

- **Gross vs net cash (Known Bug Pattern #5)** — directly relevant to today's `ServicesAmount` change. Handled correctly per the diff: `gross = amountPaid + ewt + discount + offset - overpay - servicesAmount`, explicitly avoiding double-counting the two additive-to-`AmountPaid` fields back into the gross invoice-settlement leg. Worth a second pair of eyes since it wasn't reviewed by this session — flagging per the pattern's history of being easy to get subtly wrong.
- **NULL-safety on `isCancelled`/`isReturned`** (`AddBranchOrder.cs`) — this session flagged on 2026-08-24/25 that `DeliveryDetails.isCancelled`'s default was unverified (no `CREATE TABLE` in-repo). Today's `isnull(isCancelled,0)=0` guard is a defensive fix consistent with that concern, now also extended to `isReturned`.
- **Table-type ALTER limitation** — the Conversion `ParentReferenceCode` script had to drop/recreate `tt_ConversionBarcodeSourceLines` rather than `ALTER`, and documented verifying via `sys.parameters` that nothing else referenced it first. Same class of hazard flagged in this session's own Dispatch work (`DROP TYPE` breaking redeploys) — good that it was checked before dropping this time.
- **LoadData() / Load** — not implicated today; none of today's touched forms' init sequencing changed.
- **`.Text` vs `.EditValue`**, **`DisableTextEditor`** — not implicated in today's diffs.

## Open Questions / Next Up

1. **None of today's SQL scripts are confirmed deployed** to `CORECSERP_002_DEV` or `CORECSJFC2026_STAGING` from this session's vantage point — worth confirming before relying on them, especially the `ClientPayment` GL-splitting change and the Conversion driploss/percentage-share changes, both of which affect posted financials.
2. **Carried over from 2026-08-25**: `sp_ReverseSTSInventoryTransfer` still posts its reversal GL ticket under hardcoded branch `'888'`, while `spu_PostSTSDispatch` (Dispatch Per Barcode) now posts the original dispatch ticket under the actual dynamic origin branch. A dispatch from a non-HO branch would reverse into a GL mismatch. Still unresolved — needs a decision on whether to make the reversal ticket branch dynamic too.
3. **Unreviewed external changes, flagged for visibility only** (per this session, not assessed today): `POSManagementReport.cs` (204-line diff), `POSXReadReportDevEx.cs`, `Orders/ViewBranchOrder.Designer.cs`/`.resx`.
4. **`ServicesAmount` change deserves a `ledger-integrity-auditor` pass** before considering it fully verified — it touches AR sub-ledger splitting and wasn't reviewed by any subagent this session (it was written outside this session entirely).
