# EOD Report — 2026-09-15

No commits today; all work is uncommitted in the working tree. Summary below is based on the actual diffs/new files, not git log.

## Completed

- **`SupplierPaymentDevEx.cs`** (AccountingDevEx) — fixed a `NullReferenceException` crash on supplier selection (`populate()` accessed grid columns unconditionally after a possible `SqlException`/empty result); added numeric grid formatting for `AmountPaid`/`EWTAmount`/`DiscountAmount`/`ReturnAllowances` (reused `repAmount` SpinEdit as `ColumnEdit`).
- **`Classes/DevXGridViewSettings.cs`** — added reusable `FormatNumericColumns(view, params columnNames)` helper (scoped, unlike the existing blunt `setGridFormat`); null-guarded `ShowFooterTotal` against a missing column.
- **`VoucheringManualFrm.cs`** + two new SQL scripts (`2026-09-15_VoucheringManual_VarianceFXMapping.sql`, `2026-09-15_VoucheringManual_AutoAPTradeLegs.sql`) — major redesign: added editable Amount-to-Apply/derived Variance(FX) columns; replaced the old manual AP-Trade grid-entry requirement with auto-generated AP-Trade/FX GL legs resolved via `JournalEntryMapping`; legs now stage into a temp table and net into a true compound `TicketDetails` entry per account. Went through several review rounds (`sp-reviewer`, `ui-form-reviewer`, `ledger-integrity-auditor`) with fixes applied each round (stale-balance guard, duplicate-post guard, bank-recon idempotency collision, zero-amount voucher guard, branch tagging, config-driven AP-Trade account).
- **`TicketInquiryFrm.Designer.cs`** — numeric formatting for `gridViewDetails`' Debit/Credit.
- **`ClientPaymentsDevExAcctg.cs`** — numeric formatting for `gridView2`'s InvoiceAmount/Balance/AmountPaid/OverPay/EWTAmount/DiscountAmount/OffsetAmount/ServicesAmount.
- **`AddExpenseDevExFrm.cs`** — numeric formatting for `gridViewPostedDetails`' Debit/Credit.
- **`SQL/2026-09-15_splist_Accounts_NumericNotFormat.sql`** — root-cause fix for a formatting bug that survived the `SupplierPaymentDevEx.cs` fix above: the shared SP returned `ActualCost`/`Balance`/`AmountPaid` as `FORMAT(...,'N2')` strings in places, not real `DECIMAL`. Reviewed against live STAGING, no unintended logic changes.
- **`CLAUDE.md`** — added two new Known Bug Patterns (#9 `sp_Payment_PostBankReconEntry` idempotency collision, #10 gross-must-match-fresh-balance not just internally-consistent) and one new Convention (auto-mapped compound-entry posting pattern, referencing today's `sp_PostVoucherManual` work).

## In Progress

- All three SQL scripts today are written and reviewed but **not deployed anywhere** — confirmed live STAGING still runs the original, unfixed procedures. User is deploying manually, in their own order/environment.
- Numeric-formatting sweep across report grids is ongoing at the user's direction ("mostly all... not in numeric format") — 4 grids fixed so far; more remain unaudited.
- `HOFormsDevEx/SupplierPaymentDevEx.cs` (a separate sibling file) has the identical missing `AmountPaid` format gap — flagged, not yet fixed, awaiting direction.

## Bugs Encountered (known CLAUDE.md patterns)

- **SQL `FORMAT()`-to-string anti-pattern** (documented convention: "Reporting Quantity/Amount columns must be numeric") — concretely hit in `splist_Accounts`'s EXPENSE branch; explains a subtle symptom (column only *looked* numeric while a cell was actively being edited, since the SpinEdit editor reformats a string value but the resting grid cell can't).
- **New pattern added today (#9):** `sp_Payment_PostBankReconEntry` is idempotent by `(ReferenceNo, AccountCode, ItemType)` — a second call for the same combination silently no-ops. Caused a real bug in the compound-entry redesign (two cash movements on the same bank account would have collided); fixed by netting per account before calling it.
- **New pattern added today (#10):** validating a settlement amount against the caller's own other numbers isn't the same as validating against the database's current state — added an `UPDLOCK`'d fresh-balance assertion to `sp_PostVoucherManual` to close a stale-data race.

## Open Questions / Next Up

- Deploy order for today's SQL: `VarianceFXMapping.sql` → `AutoAPTradeLegs.sql` (dependent), then `splist_Accounts_NumericNotFormat.sql` (independent) — to `CORECSERP_002_DEV` first, per project convention.
- Run the ledger-integrity-auditor's test checklist (FX loss/gain, mixed loss+gain same account, invoice+advance combo, stale-balance race, pure-manual voucher, duplicate submit) before mirroring anything to `CORECSJFC2026_STAGING`.
- Decide whether `HOFormsDevEx/SupplierPaymentDevEx.cs`'s matching `AmountPaid` gap should get the same fix.
- Continue the numeric-formatting sweep — next grid to be named by the user.
