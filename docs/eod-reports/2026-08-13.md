# EOD Report — 2026-08-13

No commits were made today (`git log --since=midnight` only shows `d47eea9 test` and `a0005c0 initial upload`, both repo-setup commits, not feature work). Today's actual work is uncommitted in the working tree, plus one SQL migration applied directly to two databases.

## Completed

- **Service Order GL posting + reversal** (`SQL/2026-08-13_ServiceOrder_GLPosting_And_Reversal.sql`) — wires the Service Order module into the mnemonic-driven GL posting engine (`JournalEntryMapping` + `sp_PostCompoundTicketSales`) that Products already use via `sp_ConfirmOrder`.
  - Schema: `Services.GLCode` (per-service revenue account); `ServiceOrderSummary` cancel-tracking columns (`CancelledBy`, `DateCancelled`, `CancelReason`, `TicketRefNo`, `RevTicketRefNo`).
  - New mnemonics: `SVC-REVENUE` (DR AR / CR service account) and its mirror `SVC-REVENUE-REV`.
  - `sp_ApproveServiceOrder` rewritten (old version backed up as `sp_ApproveServiceOrder_OLD_08132026140446`): now blocks approval if any line's Service has no `GLCode`, then posts one GL ticket per distinct `GLCode` in the order.
  - `sp_CancelApprovedServiceOrder` (new): reversal trap for the above, modeled on `sp_ReturnSalesOrder`'s DELIVERED-path reversal. Reverses from the *original ticket's TicketDetails* rather than re-reading current `Services.GLCode`.
  - **Verified against `CORECSERP_002_DEV`** and **applied + verified against `CORECSJFC2026_STAGING`** today (all prerequisite objects from the 2026-08-09 Service Order tables/SPs script were already present there — clean deploy, no errors).

- **`AddBranchOrderSTS.cs`** — delivery-details query now filters `AND isCancelled=0`, so cancelled delivery detail lines no longer show up as pending in the STS branch order screen.

## In Progress

- **Login/Main form not appearing after build+run** — under investigation, not resolved.
  - Found an orphaned `SalesInventorySystem.exe` process (PID 101228) already running with no visible window (`MainWindowHandle=0`). Since `SingleInstanceApp.IsSingleInstance = true`, any new launch from Visual Studio just signals this existing hidden/stuck instance instead of opening a new one — most likely proximate cause.
  - Two secondary risk spots flagged in `Login.cs` while tracing this, not yet confirmed as the actual hang:
    - `Login_Load` sets `this.Opacity = 0` before showing the `Connection` settings dialog when the `dbconn` registry value is missing, and never restores opacity afterward on that path — if the dialog is dismissed without saving, Login stays permanently invisible.
    - `tryCheckUpdate()` calls `GetCTRVersion().Result` synchronously on the UI thread — not a classic deadlock (uses `ConfigureAwait(false)`), but will freeze the UI for up to the default 100s `HttpClient` timeout if the version-check URL is unreachable.
  - Was about to inspect the actual `HKCU:\AAITCRE\ConnSettingsMain` registry values on this machine when the investigation was interrupted to answer other questions.

- **`AddOrder.cs`** — the credit-limit-exceeded check (`enableCreditLimit` / `accountbalance` vs `creditlimit`) is commented out, not deleted. Uncommitted. Unclear from the diff alone whether this is a deliberate, permanent change or a temporary local debugging state — see Open Questions.

- **`Login.cs`** — only a stray blank-line whitespace change; no functional diff.

## Bugs Encountered

- **Known Bug Pattern #4 (edit-vs-reversal, payment-touched guard)** applied correctly in `sp_CancelApprovedServiceOrder`: refuses to cancel if `AmountPaid > 0` on the service invoice, forcing the payment-reversal path instead of an in-place edit.
- **Known Bug Pattern #1 (LoadData vs Load)** is a live suspect in the login-form investigation above but not yet confirmed — flagging per CLAUDE.md's "check before and after any change" instruction so it isn't lost if the investigation resumes tomorrow.

## Open Questions / Next Up

1. **Login/Main form issue** — needs a decision on how to proceed: kill the orphaned process and confirm a clean relaunch works, and/or fix the `Opacity = 0` trap in `Login_Load` so a dismissed `Connection` dialog doesn't leave the form permanently invisible. Needs your go-ahead before killing the running process or editing `Login.cs` further.
2. **`AddOrder.cs` credit limit check** — confirm whether disabling it is intentional (and should be committed / cleaned up rather than left commented out) or a temporary local change that should be reverted before the next build.
3. **Uncommitted working tree** — `Login.cs`, `AddBranchOrderSTS.cs`, `AddOrder.cs` (plus rebuilt `bin`/`obj` artifacts) are all still uncommitted. Let me know when you want these staged/committed.
4. **CORECSJFC2026_STAGING** — now has today's Service Order GL posting live. Any Service rows in staging without a `GLCode` set will block approval until one is configured on the Services screen — worth a quick data check there before anyone tries to approve a Service Order in staging.
