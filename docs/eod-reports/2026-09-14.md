# EOD Report — 2026-09-14

No new commits since midnight; today's work is uncommitted (working tree). Summary below is based on `git diff`/`git status`, not git log.

## Completed

- **`Orders/AddOrder.cs` — `btnadd_Click` (Add Order line item)**
  Removed a dead DB check (`checkifExist` against `PurchaseOrderDetails.PONumber`) that could never fire true — `textEdit1.Text` at that point is always the non-consuming *peek* PONumber, only inserted at Save, and this form never reopens an existing PONumber. Also fixed a real duplicate-add bug hiding behind it: `add2()` was being called once inside the `if/else` **and** unconditionally again right after, so every "Add" click inserted the product line twice in the common path.

- **`AccountingDevEx/AddExpenseDevExFrm.cs` + SQL — PO-link cost bug (main task)**
  Bug: editing a Posted Single Expense to newly link it to a PO shipment didn't update that shipment's `Inventory.Cost`. Root cause: `SaveEditAsync()` never sent `@ShipmentNo`/`@isLinkedToPO` to `sp_EditSingleExpense`, and that proc had no such parameters — its repost call to `spu_PostExpenseV2` hardcoded `@ShipmentNo=''`/`@isLinkedToPO=0`.
  - `SQL/2026-09-14_EditSingleExpense_POCostLinkFix.sql` — added `@ShipmentNo`/`@isLinkedToPO` to `sp_EditSingleExpense`, forwarded to `spu_PostExpenseV2`; re-enabled the previously-commented-out guard that blocks editing an **already**-linked expense outright (user decision: hard-block, not silent no-op), since `spu_PostExpenseV2`'s cost incorporation is a cumulative increment and re-running it would double-add cost. Matching `BlockedReason` re-enabled in `sp_GetSingleExpenseDetails`.
  - Ran `sp-reviewer` on the change — found 2 blockers (an unguarded `@isLinkedToPO=0` + stale `@ShipmentNo` path that could falsely lock a record; `EXISTS`-only validation that didn't stop `Inventory.Cost` from being silently nulled or corrupted when `SUM(Available)` is NULL/≤0) plus minor issues (missing `SupplierID` filter on a `SupplierLedger` delete, `FORMAT()`-as-varchar convention violation). All fixed in `SQL/2026-09-14b_EditSingleExpense_ReviewFixes.sql`.
  - `AddExpenseDevExFrm.cs` `SaveEditAsync()` updated to send the two new parameters.
  - All three SQL revisions deployed to `CORECSERP_002_DEV` with timestamped `sp_rename` backups per convention; verified live via `OBJECT_DEFINITION`.

- **Posted Expenses grid — Balance column sorting as text, not value**
  Root cause: `sp_GetPostedSingleExpenses` returned `Amount`/`Balance`/`AmountPaid` via `FORMAT(...,'N2')` (varchar), so the grid sorted them alphabetically. Fixed in `SQL/2026-09-14c_GetPostedSingleExpenses_NumericColumns.sql` (`CAST(...AS DECIMAL(18,2))`, deployed to DEV) plus explicit `DisplayFormat.FormatType = Numeric` / `FormatString = "N2"` added on those three grid columns in `LoadPostedExpenses()`.

- Solution rebuilt twice via MSBuild (Debug/AnyCPU) after each C# change — compiles clean, no new errors.

## In Progress

- End-to-end click-through test of the Edit → Link-to-PO → cost-update flow in the running app has **not** been done yet — only build-verified and DB-verified via `OBJECT_DEFINITION`. User was going to test in `CORECSERP_002_DEV`, or ask for it to be driven live.

## Bugs Encountered / Known Bug Patterns Hit

- **Edit pattern depends on record shape** (CLAUDE.md pattern #4): `sp_EditSingleExpense`'s delete-and-repost is correct for Single Expense, but PO-cost-incorporation counts as "other activity that touched a posting" — extended the existing payment-block logic to also hard-block re-editing an already-PO-linked expense, falling back to Reverse instead, rather than letting a naive repost double-add cost.
- **Reporting Quantity/Amount must be numeric convention** (not in the 8-pattern list but explicit in Conventions): hit **twice** today — `sp_GetSingleExpenseDetails` (Debit/Credit) and `sp_GetPostedSingleExpenses` (Amount/Balance/AmountPaid) both pre-dated this rule and returned `FORMAT()`-ed varchar. Both fixed; this is very likely present elsewhere in older SPs too.
- **Caller-supplied parameter trust**: same spirit as the gross-vs-net pattern (#5) — `sp_EditSingleExpense` was made to sanitize/validate `@ShipmentNo`/`@isLinkedToPO` itself rather than trust the C# caller to keep them consistent.
- **SP Backup Renaming convention**: followed for all three SQL deployments today (`_OLD_<timestamp>` via `sp_rename` before each `CREATE`).

## Open Questions / Next Up

1. **`CORECSJFC2026_STAGING` not yet updated.** Per CLAUDE.md, any DEV DB change should also apply to STAGING but needs confirmation first — none of today's 3 SQL scripts have been run there yet.
2. **Noticed but not authored by this session:** `AddOrder.cs` line ~766 has `txteffectivedate.Enabled` flipped from `false` to `true` on disk, outside of anything done in this session (the file changed on disk mid-session). Flagging so it doesn't get mistaken for part of today's fix — worth confirming this was an intentional edit from elsewhere.
3. **`AddOrder.cs` line ~968** (Services tab, `txtposervices`) has a near-identical dead PurchaseOrderDetails-existence-check pattern to the one just removed at line 729 — not fixed, flagged earlier in the session in case it's wanted too.
4. Live app test of the PO-link/cost-update fix (see "In Progress") still needs to happen before this is considered fully verified.
